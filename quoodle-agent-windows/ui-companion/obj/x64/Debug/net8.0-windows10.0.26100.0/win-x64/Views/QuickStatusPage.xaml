<Page
    x:Class="Quoodle.Agent.UiCompanion.Views.QuickStatusPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Quoodle.Agent.UiCompanion.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource AppBackgroundBrush}">

    <ScrollViewer>
        <Grid Padding="28" MaxWidth="1220">
            <StackPanel Spacing="18">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Spacing="4">
                        <TextBlock Text="Quick Status" Style="{StaticResource PageTitleTextStyle}" />
                        <TextBlock Text="Live snapshot of agent health and activity." Style="{StaticResource PageSubtitleTextStyle}" />
                    </StackPanel>

                    <controls:StatusChip x:ConnectionId='19' x:Name="HealthChip" Grid.Column="1" Width="120" Margin="0,0,10,0" />
                    <controls:StatusChip x:ConnectionId='20' x:Name="ConnectionChip" Grid.Column="2" Width="150" />
                </Grid>

                <Border Style="{StaticResource CardBorderStyle}">
                    <Grid ColumnSpacing="20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.2*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="4">
                            <TextBlock Text="Device Name" Style="{StaticResource MutedTextStyle}" />
                            <TextBlock x:ConnectionId='18' x:Name="DeviceNameText" FontSize="17" FontWeight="SemiBold" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Spacing="4">
                            <TextBlock Text="Device ID" Style="{StaticResource MutedTextStyle}" />
                            <TextBlock x:ConnectionId='17' x:Name="DeviceIdText" FontWeight="SemiBold" />
                        </StackPanel>

                        <StackPanel Grid.Column="2" Spacing="4">
                            <TextBlock Text="Agent Version" Style="{StaticResource MutedTextStyle}" />
                            <TextBlock x:ConnectionId='16' x:Name="AgentVersionText" FontWeight="SemiBold" />
                        </StackPanel>

                        <StackPanel Grid.Column="3" Spacing="4">
                            <TextBlock Text="Latency" Style="{StaticResource MutedTextStyle}" />
                            <TextBlock x:ConnectionId='15' x:Name="LatencyHeaderText" FontWeight="SemiBold" />
                        </StackPanel>

                        <StackPanel Grid.Column="4" Spacing="4">
                            <TextBlock Text="Policy Hash" Style="{StaticResource MutedTextStyle}" />
                            <TextBlock x:ConnectionId='14' x:Name="PolicyHashText" FontWeight="SemiBold" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid ColumnSpacing="14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <controls:MetricCard x:ConnectionId='10' x:Name="LastSyncCard" Grid.Column="0" Tone="Info" Glyph="&#xE823;" />
                    <controls:MetricCard x:ConnectionId='11' x:Name="CpuCard" Grid.Column="1" Tone="Success" Glyph="&#xE7F4;" />
                    <controls:MetricCard x:ConnectionId='12' x:Name="MemoryCard" Grid.Column="2" Tone="Info" Glyph="&#xE950;" />
                    <controls:MetricCard x:ConnectionId='13' x:Name="EventsCard" Grid.Column="3" Tone="Warning" Glyph="&#xE7BA;" />
                </Grid>

                <Border Style="{StaticResource CardBorderStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Spacing="8">
                            <TextBlock Text="Current Activity" Style="{StaticResource MutedTextStyle}" />
                            <TextBlock x:ConnectionId='7' x:Name="ActivityText" FontSize="18" FontWeight="SemiBold" />
                            <TextBlock x:ConnectionId='8' x:Name="HeartbeatText" Style="{StaticResource PageSubtitleTextStyle}" />
                            <TextBlock x:ConnectionId='9' x:Name="ReconnectText" Style="{StaticResource PageSubtitleTextStyle}" />
                        </StackPanel>

                        <TextBlock Grid.Column="1" Text="Live" Foreground="{ThemeResource SuccessBrush}" VerticalAlignment="Top" FontWeight="SemiBold" />
                    </Grid>
                </Border>

                <StackPanel Orientation="Horizontal" Spacing="12">
                    <Button x:ConnectionId='4' Content="Open Dashboard"                         Style="{StaticResource AccentButtonStyle}" />
                    <Button x:ConnectionId='5' Content="Sync Now"                   Style="{StaticResource SecondaryButtonStyle}" />
                    <Button x:ConnectionId='6' Content="View Logs"                    Style="{StaticResource SecondaryButtonStyle}" />
                </StackPanel>

                <Border Background="{ThemeResource ChipWarningBackgroundBrush}" CornerRadius="{ThemeResource Corner.Large}" Padding="16">
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Spacing="4">
                            <TextBlock x:ConnectionId='2' x:Name="WarningTitleText" Foreground="{ThemeResource ChipWarningForegroundBrush}" FontWeight="SemiBold" />
                            <TextBlock x:ConnectionId='3' x:Name="SyncText" Style="{StaticResource PageSubtitleTextStyle}" TextWrapping="Wrap" />
                        </StackPanel>

                        <TextBlock Grid.Column="1" Text="Review" Foreground="{ThemeResource ChipWarningForegroundBrush}" VerticalAlignment="Center" />
                    </Grid>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>

