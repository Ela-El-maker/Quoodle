<Page
    x:Class="Quoodle.Agent.UiCompanion.Views.ActivityDiagnosticsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Quoodle.Agent.UiCompanion.Controls"
    xmlns:models="using:Quoodle.Agent.UiCompanion.Models"
    mc:Ignorable="d"
    Background="{ThemeResource AppBackgroundBrush}">

    <Grid Padding="28" MaxWidth="1280" RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock Text="Activity &amp; Diagnostics" Style="{StaticResource PageTitleTextStyle}" />
            <TextBlock Text="Full audit trail of WSS state, command history, and mock kernel-event projection." Style="{StaticResource PageSubtitleTextStyle}" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Padding="12">
            <StackPanel Orientation="Horizontal" Spacing="10">
                <Border Background="{ThemeResource SurfaceAltBrush}" CornerRadius="10" Padding="14,10">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="WSS Message Log" FontWeight="SemiBold" />
                        <Border Background="{ThemeResource ChipNeutralBackgroundBrush}" CornerRadius="8" Padding="8,2">
                            <TextBlock x:ConnectionId='7' x:Name="TabCountText" Foreground="{ThemeResource ChipNeutralForegroundBrush}" />
                        </Border>
                    </StackPanel>
                </Border>
                <Border Background="{ThemeResource SurfaceAltBrush}" CornerRadius="10" Padding="14,10">
                    <TextBlock Text="Command History" Style="{StaticResource PageSubtitleTextStyle}" />
                </Border>
                <Border Background="{ThemeResource SurfaceAltBrush}" CornerRadius="10" Padding="14,10">
                    <TextBlock Text="Kernel Events" Style="{StaticResource PageSubtitleTextStyle}" />
                </Border>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.2*" />
                <ColumnDefinition Width="1.1*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Padding="0">
                <Grid RowSpacing="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Padding="16" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="220" />
                        </Grid.ColumnDefinitions>

                        <TextBox x:ConnectionId='5' x:Name="SearchBox" PlaceholderText="Search type, source, or summary" IsEnabled="False" />
                        <ComboBox x:ConnectionId='6' x:Name="FilterBox" Grid.Column="1" Width="220"                                   />
                    </Grid>

                    <ListView x:ConnectionId='2' Grid.Row="1" x:Name="ActivityList" SelectionMode="None" Background="Transparent" BorderThickness="0" Padding="10">
                        <ListView.ItemTemplate>
                            <DataTemplate                                  >
                                <controls:TimelineRow x:ConnectionId='4'
                                    Margin="0,0,0,10"
                                                                               
                                                                         
                                                                       
                                                                           
                                                                              />
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                <StackPanel Spacing="12">
                    <TextBlock Text="Raw Message" Style="{StaticResource SectionTitleTextStyle}" />
                    <Border Background="{ThemeResource SurfaceAltBrush}" CornerRadius="{ThemeResource Corner.Medium}" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1" Padding="20">
                        <StackPanel Spacing="10">
                            <TextBlock Text="No row selected" FontWeight="SemiBold" />
                            <TextBlock Text="Click any timeline item after the live bridge is connected to inspect its structured envelope here." TextWrapping="Wrap" Style="{StaticResource PageSubtitleTextStyle}" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>

