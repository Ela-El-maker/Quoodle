<Page
    x:Class="Quoodle.Agent.UiCompanion.Views.DeviceDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Quoodle.Agent.UiCompanion.Controls"
    xmlns:models="using:Quoodle.Agent.UiCompanion.Models"
    mc:Ignorable="d"
    Background="{ThemeResource AppBackgroundBrush}">

    <ScrollViewer>
        <Grid Padding="28" MaxWidth="1200">
            <StackPanel Spacing="16">
                <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                    <TextBlock Text="Device Details" FontSize="28" FontWeight="SemiBold" />
                    <controls:StatusChip x:ConnectionId='23' x:Name="ConnectionChip" Width="130" />
                    <controls:StatusChip x:ConnectionId='24' x:Name="HealthChip" Width="120" />
                </StackPanel>
                <TextBlock Text="Snapshot overview grouped by identity, runtime, network, and sync health." Opacity="0.76" />
                <TextBlock x:ConnectionId='2' x:Name="SummaryText" Opacity="0.78" />

                <Grid ColumnSpacing="14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="{ThemeResource SurfaceBrush}" CornerRadius="{ThemeResource Corner.Large}" Padding="16" BorderBrush="{ThemeResource SurfaceAltBrush}" BorderThickness="1">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Identity" FontWeight="SemiBold" FontSize="16" />
                            <ItemsControl x:ConnectionId='18' x:Name="IdentityFactsList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate                               >
                                        <Grid x:ConnectionId='20' Margin="0,2">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="180" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock x:ConnectionId='21'                       Opacity="0.75" />
                                            <TextBlock x:ConnectionId='22' Grid.Column="1"                       TextWrapping="Wrap" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="{ThemeResource SurfaceBrush}" CornerRadius="{ThemeResource Corner.Large}" Padding="16" BorderBrush="{ThemeResource SurfaceAltBrush}" BorderThickness="1">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Runtime" FontWeight="SemiBold" FontSize="16" />
                            <ItemsControl x:ConnectionId='13' x:Name="RuntimeFactsList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate                               >
                                        <Grid x:ConnectionId='15' Margin="0,2">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="180" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock x:ConnectionId='16'                       Opacity="0.75" />
                                            <TextBlock x:ConnectionId='17' Grid.Column="1"                       TextWrapping="Wrap" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </Grid>

                <Grid ColumnSpacing="14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="{ThemeResource SurfaceBrush}" CornerRadius="{ThemeResource Corner.Large}" Padding="16" BorderBrush="{ThemeResource SurfaceAltBrush}" BorderThickness="1">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Network" FontWeight="SemiBold" FontSize="16" />
                            <ItemsControl x:ConnectionId='8' x:Name="NetworkFactsList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate                               >
                                        <Grid x:ConnectionId='10' Margin="0,2">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="180" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock x:ConnectionId='11'                       Opacity="0.75" />
                                            <TextBlock x:ConnectionId='12' Grid.Column="1"                       TextWrapping="Wrap" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="{ThemeResource SurfaceBrush}" CornerRadius="{ThemeResource Corner.Large}" Padding="16" BorderBrush="{ThemeResource SurfaceAltBrush}" BorderThickness="1">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Sync/Health" FontWeight="SemiBold" FontSize="16" />
                            <ItemsControl x:ConnectionId='3' x:Name="SyncFactsList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate                               >
                                        <Grid x:ConnectionId='5' Margin="0,2">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="180" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock x:ConnectionId='6'                       Opacity="0.75" />
                                            <TextBlock x:ConnectionId='7' Grid.Column="1"                       TextWrapping="Wrap" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>

