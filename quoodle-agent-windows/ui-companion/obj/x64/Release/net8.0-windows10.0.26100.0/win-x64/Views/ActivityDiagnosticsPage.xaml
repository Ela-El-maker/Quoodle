<Page
    x:Class="Quoodle.Agent.UiCompanion.Views.ActivityDiagnosticsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Quoodle.Agent.UiCompanion.Controls"
    xmlns:models="using:Quoodle.Agent.UiCompanion.Models"
    mc:Ignorable="d"
    Background="{ThemeResource AppBackgroundBrush}">

    <Grid Padding="28" MaxWidth="1200" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Activity &amp; Diagnostics" FontSize="28" FontWeight="SemiBold" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
            <TextBlock Text="Severity filter:" VerticalAlignment="Center"/>
            <ComboBox x:ConnectionId='5' x:Name="FilterBox" Width="200"                                   />
        </StackPanel>

        <ListView x:ConnectionId='2' Grid.Row="2" x:Name="ActivityList" SelectionMode="None" Background="Transparent" BorderThickness="0">
            <ListView.ItemTemplate>
                <DataTemplate                                  >
                    <controls:TimelineRow x:ConnectionId='4'
                                                                   
                                                             
                                                           
                                                               
                                                                  />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

