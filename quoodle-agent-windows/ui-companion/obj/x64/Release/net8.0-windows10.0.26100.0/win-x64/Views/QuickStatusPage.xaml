<Page
    x:Class="Quoodle.Agent.UiCompanion.Views.QuickStatusPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Quoodle.Agent.UiCompanion.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource AppBackgroundBrush}">

    <Grid Padding="28" MaxWidth="980">
        <StackPanel Spacing="16">
            <TextBlock Text="Quick Status" FontSize="28" FontWeight="SemiBold"/>
            <TextBlock Text="Live heartbeat and connection pulse from the mock state stream." Opacity="0.76" />

            <Border Background="{ThemeResource SurfaceBrush}" CornerRadius="{ThemeResource Corner.Large}" Padding="18" BorderBrush="{ThemeResource SurfaceAltBrush}" BorderThickness="1">
                <StackPanel Spacing="10">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <controls:StatusChip x:ConnectionId='8' x:Name="ConnectionChip" Width="150"/>
                        <controls:StatusChip x:ConnectionId='9' x:Name="HealthChip" Width="130"/>
                    </StackPanel>
                    <TextBlock x:ConnectionId='2' x:Name="ActivityText" />
                    <TextBlock x:ConnectionId='3' x:Name="HeartbeatText" Opacity="0.8" />
                    <TextBlock x:ConnectionId='4' x:Name="SyncText" Opacity="0.8" />
                    <TextBlock x:ConnectionId='5' x:Name="LatencyText" Opacity="0.8" />
                    <TextBlock x:ConnectionId='6' x:Name="ReconnectText" Opacity="0.8" />
                    <Button x:ConnectionId='7' Content="Sync Now"                   HorizontalAlignment="Left" Style="{StaticResource AccentButtonStyle}" />
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>

