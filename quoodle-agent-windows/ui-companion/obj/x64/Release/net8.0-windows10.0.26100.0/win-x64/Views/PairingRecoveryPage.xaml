<Page
    x:Class="Quoodle.Agent.UiCompanion.Views.PairingRecoveryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Quoodle.Agent.UiCompanion.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource AppBackgroundBrush}">

    <ScrollViewer>
        <Grid Padding="28" MaxWidth="980">
            <StackPanel Spacing="16">
                <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                    <TextBlock Text="Pairing Recovery" FontSize="28" FontWeight="SemiBold" />
                    <controls:StatusChip x:ConnectionId='9' x:Name="PairStateChip" Width="120" />
                    <controls:StatusChip x:ConnectionId='10' x:Name="ConnectionChip" Width="130" />
                </StackPanel>
                <TextBlock Text="Use recovery actions to restore connectivity before live pipe integration." Opacity="0.76" />

                <Border Background="{ThemeResource SurfaceBrush}" CornerRadius="{ThemeResource Corner.Large}" Padding="18" BorderBrush="{ThemeResource SurfaceAltBrush}" BorderThickness="1">
                    <StackPanel Spacing="10">
                        <TextBlock x:ConnectionId='2' x:Name="IdentityText" FontWeight="SemiBold" />
                        <TextBlock x:ConnectionId='3' x:Name="HealthText" Opacity="0.82" />
                        <TextBlock x:ConnectionId='4' x:Name="ActivityText" TextWrapping="Wrap" Opacity="0.82" />

                        <TextBlock Text="Latest failure context" FontWeight="SemiBold" Margin="0,8,0,0" />
                        <Border Background="{ThemeResource SurfaceAltBrush}" CornerRadius="{ThemeResource Corner.Small}" Padding="10">
                            <TextBlock x:ConnectionId='8' x:Name="FailureContextText" TextWrapping="Wrap" />
                        </Border>
                        <TextBlock Text="Recommended order: Retry connection -> Start re-pair -> Reset UI session." Opacity="0.72" />

                        <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,8,0,0">
                            <Button x:ConnectionId='5' x:Name="RetryButton" Content="Retry Connection"                           Style="{StaticResource AccentButtonStyle}" />
                            <Button x:ConnectionId='6' x:Name="RePairButton" Content="Start Re-pair"                  />
                            <Button x:ConnectionId='7' x:Name="ResetSessionButton" Content="Reset UI Session"                        />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>

