<Page
    x:Class="Quoodle.Agent.UiCompanion.Views.QuickStatusPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Quoodle.Agent.UiCompanion.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource AppBackgroundBrush}">

    <ScrollViewer>
        <Grid Padding="16,8,16,16" MaxWidth="1260">
            <StackPanel Spacing="14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Spacing="4">
                        <TextBlock Text="Quick Status" Style="{StaticResource PageTitleTextStyle}" />
                        <TextBlock Text="Live snapshot of agent health and activity." Style="{StaticResource PageSubtitleTextStyle}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Top">
                        <controls:StatusChip x:Name="HealthChip" Width="120" Visibility="Collapsed" />
                        <controls:StatusChip x:Name="ConnectionChip" Width="150" />
                    </StackPanel>
                </Grid>

                <Border Style="{StaticResource CardBorderStyle}">
                    <Grid ColumnSpacing="18">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.25*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="4">
                            <TextBlock Text="DEVICE NAME" Style="{StaticResource MutedTextStyle}" />
                            <TextBlock x:Name="DeviceNameText" FontSize="30" FontWeight="SemiBold" TextWrapping="Wrap" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Spacing="4">
                            <TextBlock Text="DEVICE ID" Style="{StaticResource MutedTextStyle}" />
                            <TextBlock x:Name="DeviceIdText" FontWeight="SemiBold" />
                        </StackPanel>

                        <StackPanel Grid.Column="2" Spacing="4">
                            <TextBlock Text="AGENT VERSION" Style="{StaticResource MutedTextStyle}" />
                            <TextBlock x:Name="AgentVersionText" FontWeight="SemiBold" />
                        </StackPanel>

                        <StackPanel Grid.Column="3" Spacing="4">
                            <TextBlock Text="UPTIME" Style="{StaticResource MutedTextStyle}" />
                            <TextBlock x:Name="LatencyHeaderText" FontWeight="SemiBold" />
                        </StackPanel>

                        <StackPanel Grid.Column="4" Spacing="4">
                            <TextBlock Text="POLICY HASH" Style="{StaticResource MutedTextStyle}" />
                            <TextBlock x:Name="PolicyHashText" FontWeight="SemiBold" TextWrapping="Wrap" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <controls:MetricCard x:Name="LastSyncCard" Grid.Column="0" Tone="Info" Glyph="&#xE823;" />
                    <controls:MetricCard x:Name="CpuCard" Grid.Column="1" Tone="Success" Glyph="&#xE7F4;" />
                    <controls:MetricCard x:Name="MemoryCard" Grid.Column="2" Tone="Info" Glyph="&#xE950;" />
                    <controls:MetricCard x:Name="EventsCard" Grid.Column="3" Tone="Warning" Glyph="&#xE7BA;" />
                </Grid>

                <Border Style="{StaticResource CardBorderStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Spacing="8">
                            <TextBlock Text="CURRENT ACTIVITY" Style="{StaticResource MutedTextStyle}" />
                            <TextBlock x:Name="ActivityText" FontSize="34" FontWeight="SemiBold" />
                            <TextBlock x:Name="HeartbeatText" Style="{StaticResource PageSubtitleTextStyle}" />
                            <TextBlock x:Name="ReconnectText" Style="{StaticResource PageSubtitleTextStyle}" />
                        </StackPanel>

                        <TextBlock Grid.Column="1" Text="Live" Foreground="{ThemeResource SuccessBrush}" VerticalAlignment="Top" FontWeight="SemiBold" />
                    </Grid>
                </Border>

                <StackPanel Orientation="Horizontal" Spacing="12">
                    <Button Content="Open Dashboard" Click="OnOpenDashboard" Style="{StaticResource AccentButtonStyle}" />
                    <Button Content="Sync Now" Click="OnSyncNow" Style="{StaticResource SecondaryButtonStyle}" />
                    <Button Content="View Logs" Click="OnViewLogs" Style="{StaticResource SecondaryButtonStyle}" />
                </StackPanel>

                <Border
                    Background="{ThemeResource WarningPanelBackgroundBrush}"
                    BorderBrush="{ThemeResource WarningPanelBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="16">
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Spacing="4">
                            <TextBlock x:Name="WarningTitleText" Foreground="{ThemeResource ChipWarningForegroundBrush}" FontWeight="SemiBold" />
                            <TextBlock x:Name="SyncText" Style="{StaticResource PageSubtitleTextStyle}" TextWrapping="Wrap" />
                        </StackPanel>

                        <TextBlock Grid.Column="1" Text="Review" Foreground="{ThemeResource ChipWarningForegroundBrush}" VerticalAlignment="Center" />
                    </Grid>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
