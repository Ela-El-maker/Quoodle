<Page
    x:Class="Quoodle.Agent.UiCompanion.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Quoodle.Agent.UiCompanion.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource AppBackgroundBrush}">

    <ScrollViewer>
        <Grid Padding="16,8,16,16" MaxWidth="1320">
            <StackPanel Spacing="14">
                <Border
                    Padding="16"
                    Background="{ThemeResource HeaderPanelBackgroundBrush}"
                    BorderBrush="{ThemeResource HeaderPanelBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="12">
                    <StackPanel Spacing="12">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                                <controls:StatusChip x:Name="ConnectionChip" Width="132" />
                                <TextBlock x:Name="IdentityText" Style="{StaticResource PageSubtitleTextStyle}" />
                            </StackPanel>

                            <TextBlock Grid.Column="1" x:Name="ActivityText" Style="{StaticResource PageSubtitleTextStyle}" Margin="10,0,0,0" />
                            <TextBlock Grid.Column="2" Text="Details" Style="{StaticResource PageSubtitleTextStyle}" />
                        </Grid>

                        <Grid ColumnSpacing="20" RowSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Grid.Column="0" Spacing="2">
                                <TextBlock Text="AGENT VERSION" Style="{StaticResource MutedTextStyle}" />
                                <TextBlock x:Name="HeroVersionText" FontWeight="SemiBold" />
                            </StackPanel>
                            <StackPanel Grid.Row="0" Grid.Column="1" Spacing="2">
                                <TextBlock Text="OS BUILD" Style="{StaticResource MutedTextStyle}" />
                                <TextBlock x:Name="HeroBuildText" FontWeight="SemiBold" />
                            </StackPanel>
                            <StackPanel Grid.Row="0" Grid.Column="2" Spacing="2">
                                <TextBlock Text="TRANSPORT" Style="{StaticResource MutedTextStyle}" />
                                <TextBlock x:Name="HeroTransportText" FontWeight="SemiBold" />
                            </StackPanel>
                            <StackPanel Grid.Row="0" Grid.Column="3" Spacing="2">
                                <TextBlock Text="RECONNECT ATTEMPTS" Style="{StaticResource MutedTextStyle}" />
                                <TextBlock x:Name="HeroReconnectText" FontWeight="SemiBold" />
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="0" Spacing="2">
                                <TextBlock Text="LAST SYNC" Style="{StaticResource MutedTextStyle}" />
                                <TextBlock x:Name="LastSyncText" FontWeight="SemiBold" />
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="1" Spacing="2">
                                <TextBlock Text="HEALTH" Style="{StaticResource MutedTextStyle}" />
                                <controls:StatusChip x:Name="HealthChip" Width="110" />
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" Spacing="2">
                                <TextBlock Text="CHANNEL" Style="{StaticResource MutedTextStyle}" />
                                <TextBlock x:Name="VersionText" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Grid ColumnSpacing="12" RowSpacing="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <controls:MetricCard
                        x:Name="CpuCard"
                        Grid.Row="0"
                        Grid.Column="0"
                        Tone="Success"
                        Glyph="&#xE945;" />
                    <controls:MetricCard
                        x:Name="MemoryCard"
                        Grid.Row="0"
                        Grid.Column="1"
                        Tone="Success"
                        Glyph="&#xEB7D;" />
                    <controls:MetricCard
                        x:Name="DiskCard"
                        Grid.Row="0"
                        Grid.Column="2"
                        Tone="Danger"
                        Glyph="&#xEA39;" />

                    <controls:MetricCard
                        x:Name="RxCard"
                        Grid.Row="1"
                        Grid.Column="0"
                        Tone="Info"
                        Glyph="&#xEA3A;" />
                    <controls:MetricCard
                        x:Name="TxCard"
                        Grid.Row="1"
                        Grid.Column="1"
                        Tone="Info"
                        Glyph="&#xE7F4;" />

                    <Border Grid.Row="1" Grid.Column="2" Style="{StaticResource CardBorderStyle}" Background="{ThemeResource SurfaceBrush}">
                        <StackPanel Spacing="8">
                            <TextBlock Text="WSS Event Feed" Style="{StaticResource SectionTitleTextStyle}" />
                            <TextBlock x:Name="TelemetrySummaryText" TextWrapping="Wrap" Style="{StaticResource PageSubtitleTextStyle}" />
                            <Button Content="Sync Now" Click="OnSyncNow" HorizontalAlignment="Left" Style="{StaticResource SecondaryButtonStyle}" />
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
